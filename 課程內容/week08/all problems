(max.80)建立一個鏈結串列，其結構如下，輸入全班同學資料，放入鏈結串列中。最後再將鏈結串列內的資料根據學號由小到大逐筆印出(可參考p.517 156~173行)。
struct stuData

{

 unsigned int StuNum;

  int math, computer;

  struct stuData *nextStu

} ;

2. (max.95) 再由鍵盤輸入學號，根據學號將該筆學生資料由此鏈結串列中刪除，若找不到該筆資料，程式也需要提醒使用者找不到。

3. 將上一題改為能夠刪除多筆重複內容的節點的程式。
